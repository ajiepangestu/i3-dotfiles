# [Default MOD Button]
set $mod Mod4

# [Default Terminal]
set $terminal urxvt -e

# [Default Arrow Key]
set $up l
set $down k
set $left j
set $right semicolon

# [Window Configuration]
for_window [class=".*"] border pixel 2

# [Font]
font pango:Iosevka 10

# [Window Configuration] [Android emulator]
for_window [title="Android Emulator - Pixel_API*"] floating enable
for_window [title="Genymotion*"] floating enable

# [Themes]
# class                 border  bground text    indicator child_border
client.focused          #282828 #EECB5A #FFFFFF #EECB5A   #EECB5A
client.focused_inactive #282828 #282828 #FFFFFF #282828   #282828
client.unfocused        #282828 #282828 #888888 #282828   #282828
client.urgent           #282828 #900000 #FFFFFF #900000   #900000
client.placeholder      #282828 #EECB5A #FFFFFF #EECB5A   #EECB5A

client.background       #282828

# [Gaps]
gaps inner 2
# gaps bottom 2

# [Volume]
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# [Audio Mixer]
bindsym $mod+g exec --no-startup-id $terminal "ncpamixer"

# [Brightness]
bindsym XF86MonBrightnessUp exec xbacklight -inc 2
bindsym XF86MonBrightnessDown exec xbacklight -dec 2

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# [Terminal]
bindsym $mod+Return exec i3-sensible-terminal

# [Network]
bindsym $mod+n exec --no-startup-id networkmanager_dmenu

# [Kill Focused Window]
bindsym $mod+Shift+q kill

# [Dmenu]
bindsym $mod+d exec --no-startup-id j4-dmenu-desktop --dmenu='rofi -dmenu -lines 6  -width 600 -i -sort -p RUN'

# [Change Focus]
bindsym $mod+$left  focus left
bindsym $mod+$down  focus down
bindsym $mod+$up    focus up
bindsym $mod+$right focus right

# [Change Focus Alternative]
bindsym $mod+Left   focus left
bindsym $mod+Down   focus down
bindsym $mod+Up     focus up
bindsym $mod+Right  focus right

# [Move Focused Window]
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# [Move Focused Window Alternative]
bindsym $mod+Shift+Left     move left
bindsym $mod+Shift+Down     move down
bindsym $mod+Shift+Up       move up
bindsym $mod+Shift+Right    move right

# [Split Horizontal]
bindsym $mod+h split h

# [Split vertical]
bindsym $mod+v split v

# [Fullscreen]
bindsym $mod+f fullscreen toggle

# [Container Layout]
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# [Tiling / Floating]
bindsym $mod+Shift+space floating toggle

# [Change Focus Tiling / Floating]
bindsym $mod+space focus mode_toggle

# [Focus Parent Container]
bindsym $mod+a focus parent

# [Focus Child Container]
bindsym $mod+Shift+a focus child

# [Workspace]
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# [Switch Workspace]
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# [Move Focused Container to Other Workspace]
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# [Reload]
bindsym $mod+Shift+c reload

# [Restart]
bindsym $mod+Shift+r restart

# [Exit]
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# [Resize]
mode "resize" {
        bindsym $left resize shrink width 10 px or 10 ppt
        bindsym $down resize grow height 10 px or 10 ppt
        bindsym $up resize shrink height 10 px or 10 ppt
        bindsym $right resize grow width 10 px or 10 ppt
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# [Move Window]
mode "move" {
	bindsym $left move left 20px
  	bindsym $down move down 20px
	bindsym $up move up 20px
  	bindsym $right move right 20px
	bindsym Left move left 20px
  	bindsym Down move down 20px
	bindsym Up move up 20px
  	bindsym Right move right 20px
  	bindsym Return mode "default"
  	bindsym Escape mode "default"
  	bindsym $mod+m mode "default"
}
bindsym $mod+m focus floating; mode "move"

# [Power Menu]
bindsym $mod+Shift+End exec --no-startup-id ~/.local/bin/rofi-power "i3-msg exit"

# [Clip Menu]
bindsym $mod+p exec --no-startup-id clipmenu
bindsym $mod+Shift+p exec --no-startup-id rm -rf /tmp/clipmenu/*

# [Polybar]
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
# [Background]
exec_always --no-startup-id feh --bg-fill -Z ~/.background/wallpaper.png
# [xbanish]
exec --no-startup-id xbanish
# [Clipmenu]
exec --no-startup-id clipmenud

